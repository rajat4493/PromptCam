# Support/Signing.xcconfig IS committed, with placeholder values, so that
# `xcodegen generate` always works. Keep your local edits out of git with:
#     git update-index --skip-worktree Support/Signing.xcconfig

# Generated by XcodeGen — regenerate with `xcodegen generate`.
*.xcodeproj/
*.xcworkspace/

# Xcode
xcuserdata/
*.xcuserstate
DerivedData/
build/
*.ipa
*.dSYM.zip

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# macOS
.DS_Store
